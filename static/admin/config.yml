backend:
  name: git-gateway
  branch: master

media_folder: static/img # Folder where user uploaded files should go
public_folder: /img

search: false

slug:
  encoding: ascii
  clean_accents: true
  sanitize_replacement: '-'

collections:
  - name: authors
    identifier_field: name
    label: Authors
    folder: content/authors
    create: true
    slug: '{{slug}}'
    summary: '{{name}}'
    fields:
      - { label: Name, name: name, widget: string }
      - {
          label: Avatar,
          name: avatar,
          widget: image,
          choose_url: true,
          allow_multiple: false,
          required: false
        }
      - { label: Email, name: email, widget: string, required: false }
      - { label: Twitter, name: twitter, widget: string, required: false }
      - { label: LinkedIn, name: linkedin, widget: string, required: false }
      - { label: Instagram, name: instagram, widget: string, required: false }
      - { label: About, name: 'body', widget: 'markdown' }
  - name: posts # Used in routes, e.g., /admin/collections/blog
    identifier_field: title
    label: Posts # Used in the UI
    folder: content/posts # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: '{{slug}}' # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: Title, name: title, widget: string }
      - { label: Subtitle, name: subtitle, widget: string, required: false }
      - { label: Original Author, name: oauthor, widget: string, required: false }
      - { label: Original Author - English, name: eauthor, widget: string, required: false }
      - { label: Title - English, name: etitle, widget: string, required: false }
      - { label: Subtitle - English, name: esubtitle, widget: string, required: false }
      - { label: Source, name: source, widget: string }
      - { label: Source - English, name: esource, widget: string }
      - { label: Link, name: link, widget: string, required: false }
      - { label: 'Publish Date', name: date, widget: date, default: '', format: 'YYYY-MM-DD' }
      - { label: Draft, name: draft, widget: boolean, default: true, required: false }
      - { label: Summary, name: summary, widget: string, required: false }
      - { label: 'Tags', name: 'tags', widget: 'list', allow_add: true }
      - {
          label: 'Featured Image',
          name: 'image',
          widget: 'image',
          choose_url: true,
          allow_multiple: false,
          required: false
        }
      - {
          label: Author,
          name: author,
          widget: relation,
          collection: authors,
          search_fields: ['name'],
          value_field: name,
          display_fields: ['name']
        }
      - { label: Body, name: 'body', widget: 'markdown' }
